import { ElementRef } from '@angular/core';
import { CustomhighliterDirective } from './customhighliter.directive';

describe('CustomhighliterDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomhighliterDirective();
    expect(directive).toBeTruthy();
  });

});